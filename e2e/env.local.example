# LocalStack E2E — copy into your shell. Run: npm run test:e2e:local

# --- Required ---
# HttpApiUrl from InvoiceProcessing-local (no trailing slash). Or: npm run playwright:print-env:local
PLAYWRIGHT_API_BASE_URL=http://127.0.0.1:4566/...

# DynamoDB poll from the test runner (use localhost if Playwright runs on the host)
AWS_ENDPOINT_URL=http://localhost:4566
AWS_REGION=us-east-1

# --- Recommended (LocalStack Community) ---
# Matches cdk.json invoice.local.presignLocalSecret
PLAYWRIGHT_PRESIGN_LOCAL_SECRET=localstack-presign-change-me

# --- Optional: Cognito/JWT instead of presign secret ---
# PLAYWRIGHT_USE_COGNITO=1
# PLAYWRIGHT_COGNITO_POOL_ID=us-east-1_xxxx
# PLAYWRIGHT_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
# PLAYWRIGHT_TEST_EMAIL=e2e-invoice@local.invalid
# PLAYWRIGHT_TEST_PASSWORD=TestPass123!

PLAYWRIGHT_INVOICES_TABLE=invoice-records-local
# PLAYWRIGHT_REVIEW_TIMEOUT_MS=120000

# Optional MailHog soft-check
# PLAYWRIGHT_MAILHOG_URL=http://localhost:8025

PLAYWRIGHT_SPA_BASE_URL=http://127.0.0.1:5173
# PLAYWRIGHT_SKIP_WEBSERVER=1
